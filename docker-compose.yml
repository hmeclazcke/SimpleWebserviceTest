services:
  postgres:
    image: postgres:16
    container_name: simple-webservice-postgres
    environment:
      POSTGRES_DB: simplewebservice
      POSTGRES_USER: hernan
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: